import XCTest
@testable import RBR360ImageViewer

final class RBR360ImageViewerTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(RBR360ImageViewer().text, "Hello, World!")
    }
}
